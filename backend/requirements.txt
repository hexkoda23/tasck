# TASCK API runtime dependencies.
#
# Every entry here is imported by application code (or is a runtime dependency
# FastAPI/Pydantic resolve at import time). Versions are pinned to the exact
# releases that were running on the previous deployment, so behaviour carries
# over unchanged.
#
# Removed for the Azure build: the Emergent LLM gateway (emergentintegrations,
# only installable from Emergent's private index), and the packages nothing in
# backend/ imports: litellm, openai, google-*/grpcio, huggingface/tokenizers,
# tiktoken, pandas, numpy, boto3, stripe, aiohttp, jq, PyYAML, Jinja2, jwt/jose,
# bcrypt/passlib, black/flake8/mypy/isort.

# Web framework and server
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
h11==0.16.0
python-multipart==0.0.22
anyio==4.12.1
sniffio==1.3.1
idna==3.11
click==8.3.1

# Data models
pydantic==2.12.5
pydantic_core==2.41.5
annotated-types==0.7.0
typing_extensions==4.15.0
typing-inspection==0.4.2
email-validator==2.3.0
dnspython==2.8.0

# MongoDB
motor==3.3.1
pymongo==4.5.0

# Configuration
python-dotenv==1.2.1

# Outbound HTTP (Anthropic API, SerpAPI, brand scraping)
requests==2.32.5
urllib3==2.6.3
charset-normalizer==3.4.4
certifi==2026.2.25
httpx==0.28.1
httpcore==1.0.9

# Document generation (DOCX, PDF, flipbook, XLSX import)
python-docx==1.2.0
lxml==6.1.1
pypdf==6.15.0
reportlab==4.5.1
pillow==12.1.1
openpyxl==3.1.5
et_xmlfile==2.0.0

# Time zones on slim images (zoneinfo has no system tzdata in python:*-slim)
tzdata==2025.3
python-dateutil==2.9.0.post0
six==1.17.0
